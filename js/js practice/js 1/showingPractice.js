const taskManaging = document.querySelector("newTask");
const taskClick = document.querySelector("addTaskBtn");
const taskAll = document.querySelector("taskList");

taskClick.addEventListener("click", taskAdd);

function taskAdd() {
  const taskFUll = newTaskInput.value.trim();
}
